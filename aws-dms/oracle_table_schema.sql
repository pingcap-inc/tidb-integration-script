CREATE TABLE github_events (
  id NUMBER DEFAULT NULL,
  type VARCHAR(255) DEFAULT NULL,
  created_at VARCHAR(255) DEFAULT NULL,
  repo_id NUMBER DEFAULT NULL,
  repo_name VARCHAR(255) DEFAULT NULL,
  actor_id NUMBER DEFAULT NULL,
  actor_login VARCHAR(255) DEFAULT NULL,
  actor_location VARCHAR(255) DEFAULT NULL,
  language VARCHAR(255) DEFAULT NULL,
  additions NUMBER DEFAULT NULL,
  deletions NUMBER DEFAULT NULL,
  action VARCHAR(255) DEFAULT NULL,
  num NUMBER DEFAULT NULL,
  commit_id VARCHAR(255) DEFAULT NULL,
  comment_id NUMBER DEFAULT NULL,
  org_login VARCHAR(255) DEFAULT NULL,
  org_id NUMBER DEFAULT NULL,
  state VARCHAR(255) DEFAULT NULL,
  closed_at VARCHAR(255) DEFAULT NULL,
  comments NUMBER DEFAULT NULL,
  pr_merged_at VARCHAR(255) DEFAULT NULL,
  pr_merged NUMBER DEFAULT NULL,
  pr_changed_files NUMBER DEFAULT NULL,
  pr_review_comments NUMBER DEFAULT NULL,
  pr_or_issue_id NUMBER DEFAULT NULL,
  event_day VARCHAR(255) DEFAULT NULL,
  event_month VARCHAR(255) DEFAULT NULL,
  author_association VARCHAR(255) DEFAULT NULL,
  event_year NUMBER DEFAULT NULL,
  push_size NUMBER DEFAULT NULL,
  push_distinct_size NUMBER DEFAULT NULL,
  creator_user_login VARCHAR(255) DEFAULT NULL,
  creator_user_id NUMBER DEFAULT NULL,
  pr_or_issue_created_at VARCHAR(255) DEFAULT NULL
);